name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-37'
     - 'test-gcp-qa'

env:
  tag: ${{ github.ref }}
  key: ${{ secrets.KEY }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Environment Variable
      run: |
        cat ${{ secrets.KEY }} > file.json
        cat file.json

