name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-25'
     - 'test-gcp-qa'

env:
  tag: ${{ github.ref }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Environment Variable
      run: |
        echo "a=tanik" >> $GITHUB_ENV

    - name: Use the value
      run: |
        echo "${{ env.a }}"

