name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-34'
     - 'test-gcp-qa'

env:
  tag: ${{ github.ref }}
  key: ${{ secrets.KEY }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Environment Variable
      run: |
        den=leo
        echo "a=$key" >> $GITHUB_ENV

    - name: Use the value
      run: |
        echo "${{ env.a }}"

