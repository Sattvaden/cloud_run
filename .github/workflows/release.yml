name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-32'
     - 'test-gcp-qa'

env:
  tag: ${{ github.ref }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Environment Variable
      run: |
        den=leo
        echo "a=$tag" >> $GITHUB_ENV

    - name: Use the value
      run: |
        echo "${{ env.a }}"

