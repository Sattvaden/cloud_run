name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-36'
     - 'test-gcp-qa'

env:
  tag: ${{ github.ref }}
  key: ${{ secrets.KEY }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Environment Variable
      run: |
        cat ${{ secrest.KEY }} > file.json
        cat file.json

