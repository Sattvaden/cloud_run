name: Denis

on:
  push:
    tags:
     - 'test-gcp-dev-47'
     - 'test-gcp-qa'
     - 'release-plugin-[0-9]+.[0-9]+.[0-9]+*'

env:
  tag: ${{ github.ref }}
  key: ${{ secrets.KEY }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set ${{ github.ref }}
      run: echo den

